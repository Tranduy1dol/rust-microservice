[package]
name = "entities"
version = "0.1.0"
edition = "2021"

[dependencies]
sea-orm = { version = "1.1.10", features = [
    "sqlx-postgres",
    "runtime-tokio-rustls",
] }
rust_decimal = "1.34.1"
strum = { version = "0.27.2", features = ["derive"] }
