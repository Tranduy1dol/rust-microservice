[package]
name = "entities"
version = "0.1.0"
edition = "2021"

[dependencies]
rust_decimal = "1.34.1"
sea-orm = { version = "1.1.10", features = [
  "sqlx-postgres",
  "runtime-tokio-rustls",
] }
strum = { version = "0.27.2", features = ["derive"] }
